import type { TestimonialRow } from "@/types/database";
import { saveTestimonial } from "@/features/content/cms-actions";

export function TestimonialEditor({ record }: { record?: TestimonialRow }) {
  return <form className="editor-form" action={saveTestimonial}><section className="editor-panel"><div className="editor-panel-title"><h2>Testimonial details</h2><p>Publish only genuine client feedback with written approval.</p></div><div className="editor-fields">{record && <input type="hidden" name="id" value={record.id} />}<label className="field-wide"><span>Quote</span><textarea name="quote" rows={7} minLength={20} maxLength={1000} defaultValue={record?.quote ?? ""} required /></label><label><span>Person name</span><input name="personName" defaultValue={record?.person_name ?? ""} required /></label><label><span>Role</span><input name="personRole" defaultValue={record?.person_role ?? ""} /></label><label><span>Organization</span><input name="organization" defaultValue={record?.organization ?? ""} /></label><label><span>Display order</span><input name="sortOrder" type="number" min={0} defaultValue={record?.sort_order ?? 100} /></label></div></section><aside className="editor-panel editor-publish"><div className="editor-panel-title"><h2>Approval</h2><p>Approved testimonials appear publicly.</p></div><div className="editor-fields"><label className="checkbox-field"><input name="approved" type="checkbox" defaultChecked={record?.is_approved ?? false} /><span>Approved for publication</span></label><label className="checkbox-field"><input name="featured" type="checkbox" defaultChecked={record?.is_featured ?? false} /><span>Feature on homepage</span></label><button className="button button-primary">Save testimonial</button></div></aside></form>;
}
