import Link from "next/link";
import { ArrowUpRight, Plus } from "lucide-react";
import { createClient } from "@/lib/supabase/server";
export default async function AdminSolutionsPage() { const supabase = await createClient(); const { data } = await supabase.from("solutions").select("id, title, category, slug, status, is_featured, sort_order, updated_at").order("sort_order"); return <><header className="admin-page-header"><div><span>Website content</span><h1>Solutions</h1><p>Manage solution showcases, benefits, ordering, and publication.</p></div><Link className="button button-primary" href="/admin/solutions/new"><Plus size={17} /> Add solution</Link></header><section className="admin-panel"><div className="admin-table-wrap"><table><thead><tr><th>Solution</th><th>Category</th><th>Status</th><th>Order</th><th>Updated</th><th /></tr></thead><tbody>{data?.map((item) => <tr key={item.id}><td><strong>{item.title}</strong><small>/{item.slug}</small></td><td>{item.category ?? "Uncategorized"}</td><td><span className={`status-pill ${item.status}`}>{item.status}</span></td><td>{item.sort_order}</td><td>{new Intl.DateTimeFormat("en-ZW", { dateStyle: "medium" }).format(new Date(item.updated_at))}</td><td><Link href={`/admin/solutions/${item.id}`}>Edit <ArrowUpRight size={14} /></Link></td></tr>)}</tbody></table></div>{!data?.length && <div className="admin-empty">No solutions have been created.</div>}</section></>; }
